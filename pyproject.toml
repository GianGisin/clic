[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "clic"
version = "0.1.0"
dependencies = []

[project.optional-dependencies]
dev = ["ruff"]

[project.scripts]
clic = "clic.__main__:main"
